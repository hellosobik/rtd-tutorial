[build-system]
requires = ["flit_core >=3.2,<4"]
build-backend = "flit_core.buildapi"

[project]
name = "lumache"
authors = [{name = "Shoubhik", email = "sobik@calsci.io"}, {name = "Ganesh", email = "ganesh@calsci.io"}]
dynamic = ["version", "description"]
